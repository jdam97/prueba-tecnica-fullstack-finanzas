{
    "buildCommand": "prisma generate && next build",
    "functions": {
      "pages/api/auth/[...all].js": {
        "maxDuration": 30
      }
    },
    "env": {
      "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true"
    }
  }